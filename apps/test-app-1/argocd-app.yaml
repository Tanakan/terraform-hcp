apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-app-1
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: https://github.com/Tanakan/terraform-hcp
    targetRevision: main
    path: apps/test-app-1
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
